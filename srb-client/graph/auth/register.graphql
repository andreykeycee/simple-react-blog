mutation register (
  $email: String!
  $name: String
  $password: String!
) {
  register (
    request: {
      email: $email
      name: $name
      password: $password
    }
  ) {
    user {
      name
      email
    }
    error {
      type
      message
    }
    token
  }
}